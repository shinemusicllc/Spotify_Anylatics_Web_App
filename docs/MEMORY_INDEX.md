# SpotiCheck Memory Index

## Start Here

- Read `AGENTS.md`, then this file, then `docs/PROJECT_BRIEF.md`.
- For UI work, also read `docs/UI_SYSTEM.md`.
- For backend/export formatting work, read the relevant `backend/AGENTS.md` and focused tests.

## Routing

- Frontend dashboard, rows, groups, hero, filters: `frontend/app.js`, `frontend/style.css`, `frontend/index.html`, `docs/UI_SYSTEM.md`.
- Spotify fetch and image/source normalization: `backend/app/services/spotify_client.py`.
- Public API and auth contracts: `backend/app/api/`.
- VPS deployment and Caddy/Docker runtime: `deploy/`.

## Avoid By Default

- Do not read full `docs/WORKLOG.md`, full `docs/CHANGELOG.md`, or old decision history unless the task needs historical detail.
- Do not migrate long logs into this memory layer.
